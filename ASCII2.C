//C program to find ASCII Character through value supplied by user
#include<stdio.h>
int main() {
    int x;
    printf("Enter Value:");
    scanf("%d",&x);
    printf("ASCII Character is %c",x);
    return 0;
}